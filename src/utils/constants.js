export const API_URL = '/wp-json/sweb-requests';

export const PROCESS_STATE = Object.freeze({
    WAITING: 'waiting',
    LOADING: 'loading',
    CONFIRMED: 'confirmed',
    ERROR: 'error'
});
